"""Bimanual (multi-agent) task collection.

Tasks here declare one robot per agent so the canonical replay pipeline
(``scripts/advanced/replay_demo.py``) loads every agent's trajectory slice. See
``docs/source/dataset_benchmark/dataset/multiagent.md``.
"""
